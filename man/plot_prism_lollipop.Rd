% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.plot.R
\name{plot_prism_lollipop}
\alias{plot_prism_lollipop}
\title{Prism-style lollipop plot (grouped or single series)}
\usage{
plot_prism_lollipop(
  df,
  x_var,
  y_var,
  group_var = NULL,
  color_palette = NULL,
  y_label = "Value",
  y_as_percent = F,
  plot_title = NULL,
  segment_width = 1,
  point_size = 4,
  point_stroke = 1
)
}
\arguments{
\item{df}{Data frame containing the data.}

\item{x_var}{Bare column name for the x-axis (categorical variable).}

\item{y_var}{Bare column name for the y-axis (numeric variable).}

\item{group_var}{Optional bare column name for grouping; if NULL, a single series is plotted.}

\item{color_palette}{Named vector of colors for each group; for single-series, first color is used.}

\item{y_label}{Label for the y-axis.}

\item{y_as_percent}{Logical; if TRUE, scales y_var by 100 and formats labels as percentages.}

\item{plot_title}{Optional plot title.}

\item{segment_width}{Numeric; line width of the lollipop segments. Default is 1.}

\item{point_size}{Numeric; size of the lollipop points. Default is 4.}

\item{point_stroke}{Numeric; stroke width of point borders. Default is 1.}
}
\value{
A \code{ggplot2} object.
}
\description{
Create a lollipop plot with Prism-style x-axis brackets, suitable for grouped or single-series data.
}
\examples{
# Single-series example
df_single <- data.frame(Stage = LETTERS[1:5], Rate = c(0.12, 0.14, 0.2, 0.17, 0.22))
plot_prism_lollipop(df_single, Stage, Rate, color_palette = c("#0072B2"), y_label = "Conversion Rate (\%)",
                    y_as_percent = TRUE, plot_title = "Single Series Lollipop Plot")

# Grouped example
df_grouped <- data.frame(Category = rep(LETTERS[1:5], 2), Score = runif(10, 0.3, 0.6),
                         Group = rep(c("G1", "G2"), each = 5))
pal <- c("G1" = "#E69F00", "G2" = "#56B4E9")
plot_prism_lollipop(df_grouped, Category, Score, Group, color_palette = pal, y_label = "Score (\%)",
                    y_as_percent = TRUE, plot_title = "Grouped Lollipop Plot")
}
