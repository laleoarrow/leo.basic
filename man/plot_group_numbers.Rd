% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.plot.R
\name{plot_group_numbers}
\alias{plot_group_numbers}
\title{Plot numeric values by group with jitter / beeswarm, color / shape / size mapping}
\usage{
plot_group_numbers(
  df,
  group,
  number,
  color_col = NULL,
  color_rule = NULL,
  shape_col = NULL,
  shape_rule = NULL,
  size_col = NULL,
  size_rule = NULL,
  jitter = c("no", "yes", "bee"),
  x_axis_pos = c("default", "zero"),
  mean_type = c("none", "point", "line"),
  legend = TRUE
)
}
\arguments{
\item{df}{data.frame.}

\item{group}{column name (chr) mapped to x-axis categories.}

\item{number}{column name (chr) mapped to y-axis numeric values.}

\item{color_col}{column name (chr) for point color (skip \code{color_rule}).}

\item{color_rule}{function(df) → color vector; ignored if \code{color_col} given.}

\item{shape_col}{column name (chr) for point shape (skip \code{shape_rule}).}

\item{shape_rule}{function(df) → shape vector; ignored if \code{shape_col} given.}

\item{size_col}{column name (chr) for point size (skip \code{size_rule}).}

\item{size_rule}{function(df) → size vector; ignored if \code{size_col} given.}

\item{jitter}{one of \code{"no"}, \code{"yes"}, \code{"bee"}.}

\item{x_axis_pos}{\code{"default"} or \code{"zero"} (draw baseline at y = 0).}

\item{mean_type}{\code{"none"}, \code{"point"}, \code{"line"}.}

\item{legend}{Logical. Whether to show legends for colour/shape/size mappings. Default TRUE.
Set FALSE to hide all legends.}
}
\value{
ggplot object.
}
\description{
Draw a grouped scatter of numeric values with optional jitter /
beeswarm arrangement, color / shape / size encoding, plus optional mean indicator.
}
\note{
\strong{Shape tips} (ggplot shape codes):
• 24 ▲, 25 ▼, 22 ■  — filled; outline via \code{stroke}.
•  0 □,  2 △,  6 ▽  — hollow.
•  3 “+”,  4 “×”,  1 ○ — thin strokes, suit overlaps.
• 21–25 accept border (\code{colour}) + fill.
For filled point with black outline: shape = 21, \code{colour = "black"}, \code{fill = "<fill>"}, tweak \code{stroke}.
Provide *_col or *_rule, or leave both NULL → defaults (colour: sig/pos = red, sig/neg = blue, else grey; shape 16; size 2).
}
\examples{
set.seed(1)
df <- data.frame(
  var        = paste0("gene", 1:60),
  group      = sample(c("Tcell","Bcell","NK"), 60, TRUE),
  aver_logFC = rnorm(60, 0, 2),
  p_val      = runif(60),
  my_shape   = sample(c(24,25,22), 60, TRUE),
  my_size    = sample(c(2,3,4), 60, TRUE)
)
plot_group_numbers(df,
                   group      = "group",
                   number     = "aver_logFC",
                   shape_col  = "my_shape",
                   size_col   = "my_size",
                   jitter     = "bee",
                   x_axis_pos = "zero",
                   mean_type  = "point")
}
